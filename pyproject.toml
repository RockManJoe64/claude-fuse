[project]
name = "claude-fuse"
version = "0.1.0"
description = "Claude Code hooks for measuring telemetry and usage with Langfuse"
requires-python = ">=3.13"
dependencies = [
    "langfuse>=3.14.1",
]

[dependency-groups]
dev = [
    "pytest>=9.0.2",
]
